# 后端

此目录用于 FastAPI 服务、预应力计算核心及自动化测试。

## DXF 导入约定

`POST /api/import/dxf` 仅接收一个 DXF 文件，读取模型空间中名为“立面”和“平面”的图层。每个图层必须且只能有一条开放二维多段线；直线段与带 bulge 的圆弧段会转换为网页使用的 `x / y / b` 节点。

导入默认按 m 处理，也可选择 mm。系统会自动反转为 x 递增方向，并校核平面、立面的首尾 x 坐标是否在 0.001 m 容差内一致。
